@model IEnumerable<CountryCityApp.Models.Country>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

<h1>Search Part</h1>
@using (Html.BeginForm("Index", "Country", null, FormMethod.Post, new { enctype = "multipart/form-data" }))
{
    @Html.AntiForgeryToken()


    <div class="form-group">
      
        <div class="col-md-10">
            @Html.TextBox("text")
            <input type="submit" value="search">
        </div>
        
    </div>
    
}

@*@using (Html.BeginForm())
{
    <fieldset>

        @Html.DropDownList("countryName")

        <p>

            <input type="submit" value="Submit" />

        </p>

    </fieldset>


}*@

<table class="table">
    @*<tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.About)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NoOfCities)
        </th>

        <th></th>
    </tr>*@

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.About)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Cities.Count)
        </td>

        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CountryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.CountryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CountryId })
        </td>
    </tr>
}

</table>
